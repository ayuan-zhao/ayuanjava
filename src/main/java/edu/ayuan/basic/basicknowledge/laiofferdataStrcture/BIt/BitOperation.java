package edu.ayuan.basic.basicknowledge.laiofferdataStrcture.BIt;

public class BitOperation {

    /*
    bitwise and (ampersand和号)位运算，
    if (true 1 && true 2),有逻辑短路功能

    or if(true1 ||true2)
    ~ not
    ^XOR抑或
    << left shift 左移，除右侧补充零，
    <<	左移	各二进位全部左移若干位，高位丢弃，低位补0
    若左移时舍弃的高位不包含1，则每左移一位，相当于该数乘以2
    比如 左移三位，，左侧的三个数去掉，在右侧补充零，左移1 乘以2，左移2，乘以4，左移3 乘以8
    >>右移	各二进位全部右移若干位，对无符号数，高位补0，有符号数，各编译器处理方法不一样，有的补符号位（算术右移），有的补0（逻辑右移）
    -1,0,右移不变
    timw complexity of these basic operations O(1)
    &所有0 都是0，
    任何一位，or上 0都不变
     */

    public static void main(String[] args) {
//        int a = -50;
//        int a1 = -104;
//        int c = a & a1;
        byte a;
        byte b;


//        System.out.println("a: " + a + "," + "a1: " + a1 + "c:" + c);


    }

}
