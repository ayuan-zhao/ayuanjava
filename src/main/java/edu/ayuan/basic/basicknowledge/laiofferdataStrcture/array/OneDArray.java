package edu.ayuan.basic.basicknowledge.laiofferdataStrcture.array;
//1. once created the length of the array is fixed长度必须固定
//    2.an array can only contains single type of value数据类型必须完全一样 （python可以放不同类型的）
//we can use index to fetch an element from the array ,index starts from 0下标从零开始，
//   下标是和起始位置的偏移量，第一个元素和第一个元素的偏移是0
//    between this element and the first element in the array
/*

 */
public class OneDArray {
//    数组创建是在heap上创建，是一个object,array 存储的是heap上的地址，
    int[] array = new int[4];//初始化 array = {0,0,0,0}
//Car[] carArray = new Car[4];
}
